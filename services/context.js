import { createContext } from "react";

export const UserContext = createContext(null);

export const UserDocContext = createContext(undefined);

export const showNavOutContext = createContext(false);
